# SportsBook
A sports data scraping and analysis tool

This project is in its very early days and currently only supports football (soccer).

Fearure list:

* Import leagues and fixtures from one of two available online sources.

* See a visual comparison of the teams playing in any game.

* Run a benchmark comparison on a selected fixture.
  * This allows you to see the results of games where both teams have played the same opponent.
  * It shows results where both teams faced the same opponent at home, then away and then where
  the home team have faced the opponent at home and the away team have faced the opponent away.
  * Only games where three of the above comparisons are shown. If the home team, at home, haven't
  played the same team as the awyay team have played away on three occasions, the game is ommitted
  from predictions.

* Run manual comparison of two teams from any loaded leagues generating (somewhat inaccurate) predictions.

* Run predictions on all loaded fixtures (more accurate as the teams are guaranteed to be from the same leagues).
  * Select from using all available league data to compare home and away goal scoring and conceding form or only using data from games
  where the home team has played the same team at home as the away team as played away, making the data a fairer representation of the
  team's capabilities.

* Filter predictions to show games where specific requirements are met (eg. prediction of home side winning by 2 goals).

* Filter filtered predictions further with other filters.

 14.02.26
 Ось результати аналізу та стратегія трансформації для проекту **SportsBook**, підготовлені у форматі для Notion.

---

# 📑 Звіт AI-консультанта: Проект "SportsBook"

## 🧬 Частина 1: "ДНК" Проекту

Проект **SportsBook** — це інструмент для збору та аналізу спортивних даних (наразі зосереджений на футболі), який використовує статистичні методи для прогнозування результатів матчів. Його логіку можна розбити на такі атомарні функції:

*   **Імпорт та збір даних (Data Ingestion):** Отримання даних про ліги та розклад матчів (fixtures) з двох онлайн-джерел або завантаження з локальних JSON-файлів.
*   **Бенчмаркінг (Benchmark Comparison):** Унікальна функція порівняння команд через "спільних суперників". Система аналізує результати ігор, де обидві команди зустрічалися з одним і тим самим опонентом, щоб нівелювати різницю в силі ліг.
*   **Прогнозування (Prediction Engine):** Генерація прогнозів на основі аналізу забитих/пропущених м'ячів вдома та на виїзді. Модель дозволяє порівнювати як загальні дані ліги, так і специфічні вибірки за результатами ігор проти спільних суперників.
*   **Багатоступенева фільтрація (Advanced Filtering):** Система фільтрів для відбору матчів за конкретними критеріями (наприклад, перемога господаря мінімум у 2 голи) та використання спеціальних фільтрів від сторонніх розробників.
*   **Експорт та звітність (Reporting):** Генерація детальних електронних таблиць (spreadsheets) з прогнозами та поточною статистикою команд, а також експорт даних у формат JSON.

### 💎 Головна технічна цінність
Головна цінність проекту полягає в **алгоритмі порівняння через спільних суперників (benchmark comparison)**. Це забезпечує більш чесне представлення можливостей команд, ніж стандартні таблиці перемог/поразок, оскільки враховує контекст складності календарю для обох опонентів.

---

## 🚀 Частина 2: "Трансформація" (Інтеграція з Gemini LLM)

Додавання Gemini перетворює "статистичний калькулятор" на **інтелектуальну платформу спортивної експертизи**.

### Як зміниться функціонал?
1.  **Природно-мовний інтерфейс (NLQ):** Замість налаштування фільтрів вручну, ви зможете просто написати: *"Знайди мені найбільш надійні матчі на вихідні, де команди мають стабільну гостьову форму"*. Gemini самостійно активує потрібні функції проекту.
2.  **Синтез "Сухих" даних та Контексту:** Gemini може брати статистичні прогнози зі `SportsBook` і доповнювати їх аналізом новин (травми, зміни тренерів), створюючи цілісну картину, недоступну суто математичній моделі.
3.  **Автоматизована розсилка з інсайтами:** Бот не просто генерує таблицю, а створює читабельні звіти: *"В матчі А проти Б модель бачить високу ймовірність тоталу більше 2.5, оскільки обидві команди грають результативно проти закритих суперників"*.

### Сценарій сервісу (SportsBook + ID_001 + Gemini)

Уявімо, що ваші базові скрипти (**ID_001**) відповідають за архітектуру вашого сайту (авторизація користувачів, обробка платежів, відображення контенту).

**Алгоритм роботи готового сервісу:**
1.  **Збір (SportsBook):** Скрипт щоночі оновлює дані ліг та результати матчів через API.
2.  **Генерація (SportsBook):** Система розраховує прогнози для всіх завантажених фікстур, використовуючи бенчмаркінг.
3.  **Аналіз (Gemini):** Gemini аналізує згенерований JSON з прогнозами, відбирає "Gold" варіанти з найвищою імовірністю та пише до кожного коротке текстове обґрунтування.
4.  **Публікація (ID_001):** Ваші скрипти отримують готовий аналітичний пак (статистика + текст) і автоматично виводять його у стрічку новин вашого сайту або надсилають у Telegram підписникам.

---

## 📋 План дій для Notion
| Крок | Дія | Результат |
| :--- | :--- | :--- |
| **1** | Розширення `SportsBook` для експорту в формат, зрозумілий Gemini | Автоматизована передача даних моделі |
| **2** | Створення бібліотеки промптів для інтерпретації бенчмарк-даних | Перехід від цифр до експертних висновків |
| **3** | Налаштування API-зв'язку між результатами аналізу та ID_001 | Безшовне оновлення контенту на сайті |
| **4** | Впровадження чат-інтерфейсу для користувачів сайту | Підвищення залученості через AI-консультанта |

> **Технічна замітка:** Оскільки проект повністю написаний на **Python**, він ідеально підходить для розширення. Використання `Pipfile` спрощує додавання бібліотек для роботи з Gemini API, забезпечуючи ізольованість та стабільність вашого майбутнього сервісу.

* Filter predictions using special filters (either produced by guest contributors or specially designed filters for specific bet types).

* Display filtered predictions and all predictions on screen.

* Change the range of dates or games the predictions cover.

* Display results from throughout the whole current season of each league.

* Change the range of dates the results cover.

* Produce a spreadsheet of all predictions or filtered predictions with a wealth of current stats for each team in each prediction.

* Export currently loaded league data to a JSON file.

* Import the league data from a JSON file.

The project is growing fairly quickly. I'd love to hear what your thoughts are and even keep you up to date with new features if you like. Join the Slack group here if you're interested:

https://join.slack.com/t/sportsbookgroup/shared_invite/enQtNDc4MjYwNzMwNzg4LTAzMDk0MDM3OWFiMGJhZWU2MzAyMzQyNGI4OTlhNjgxMWRlNTZjOTAzMTM3ODdhMDIxNDU3YjI2MzM4OTlmZjg

It'd be great to hear from you so please pop in and say hi!
